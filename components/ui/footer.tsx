export function Footer() {
  return (
    <div
      className="mt-8 py-2.5 border-t text-[10px] flex justify-between flex-wrap gap-1"
      style={{ borderColor: "var(--border-hairline)", color: "var(--text-faint)" }}
    >
      <span>Kanban Simulation Training Platform</span>
      <span>Based on the Kanban Guide</span>
    </div>
  );
}
